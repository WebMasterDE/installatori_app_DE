import 'package:flutter/material.dart';

class CondominiPage extends StatelessWidget {
  static const route = '/condomini';

  const CondominiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Condomini Page'),
      ),
    );
  }
}